name: ci

on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Test and build backend
        run: mvn -f sge-backend/pom.xml -B test package

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        working-directory: sge-frontend
        run: npm install
      - name: Test frontend
        working-directory: sge-frontend
        run: npm test
      - name: Build frontend
        working-directory: sge-frontend
        run: npm run build
